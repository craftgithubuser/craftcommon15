create schema StudnetsInfo;
use StudentsInfo;

create table Students(
StudentId int,
StduentName varchar(255),
ParentName varchar(25),
Address varchar(25),
PostalCode int,Students
city varchar(255));

create database school_db;
use school_db;


create table courses(
course_id int,
title varchar(100),
number_of_credit int,
course_code varchar(20)
);